apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: excalidash
spec:
  secretName: excalidash-tls
  dnsNames:
  - "excalidash.techwerkers.nl"
  issuerRef:
    name: acme
    kind: ClusterIssuer
