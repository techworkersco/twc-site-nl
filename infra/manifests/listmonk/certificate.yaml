apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: listmonk
spec:
  secretName: listmonk-tls
  dnsNames:
  - "listmonk.techwerkers.nl"
  issuerRef:
    name: acme
    kind: ClusterIssuer
